{
  "extends": "./tsconfig.json",
  "exclude": [
    "node_modules",
    "tests",
    "**/*.spec.ts",
    "**/*.test.ts"
  ],
  "compilerOptions": {
    // You can override specific compiler options for the build here if necessary
  }
}
